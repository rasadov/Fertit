<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Email Sent</title>
    <style>
        body { font-family: Arial, sans-serif; text-align: center; margin-top: 100px; }
        .container { display: inline-block; padding: 40px; border: 1px solid #ddd; border-radius: 8px; }
        .success { color: green; font-size: 1.5em; margin-bottom: 20px; }
        button { padding: 10px 20px; font-size: 1em; border-radius: 4px; border: none; background: #007bff; color: #fff; cursor: pointer; }
        button:hover { background: #0056b3; }
    </style>
</head>
<body>
    <div class="container">
        <div class="success">Email was successfully sent!</div>
        <button onclick="window.location.href='/admin'">Go Back</button>
    </div>
</body>
</html>